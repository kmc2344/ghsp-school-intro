---
const { label, heading, copy } = Astro.props;
---

<header class="section-heading" data-reveal>
  <p class="section-label">{label}</p>
  <h2>{heading}</h2>
  {copy && <p class="section-copy">{copy}</p>}
</header>
const { label, heading, copy } = Astro.props;

<header class="section-heading" data-reveal>
  <p class="section-label">{label}</p>
  <h2>{heading}</h2>
  {copy && <p class="section-copy">{copy}</p>}
</header>
const { label, heading, copy } = Astro.props;

<header class="section-heading" data-reveal>
  <p class="section-label">{label}</p>
  <h2>{heading}</h2>
  {copy && <p class="section-copy">{copy}</p>}
</header>
